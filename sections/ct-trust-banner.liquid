{% schema %}
{
  "name": "Feature Highlights",
  "tag": "section",
  "class": "ct-custom-features",
  "settings": [],
  "blocks": [
    {
      "type": "feature",
      "name": "Feature",
      "settings": [
        {
          "type": "html",
          "id": "icon_svg",
          "label": "SVG Icon"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Description"
        }
      ]
    }
  ],
  "max_blocks": 4,
  "presets": [
    {
      "name": "Feature Highlights",
      "blocks": []
    }
  ]
}
{% endschema %}

{% stylesheet %}
.custom-features {
  background: linear-gradient(to right, #ffffff, #eaf9fd);
  padding: 40px 15px;
}

.custom-features .features-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 30px;
}

.custom-features .feature-block {
  text-align: left;
  font-family: inherit;
}

.custom-features .feature-block .svg-icon {
  height: 40px;
  margin-bottom: 15px;
}

.custom-features .feature-block h3 {
  font-weight: bold;
  font-size: 1.1rem;
  margin-bottom: 10px;
}

.custom-features .feature-block p {
  font-size: 1rem;
  margin: 0;
}

/* Mobile styles */
@media screen and (max-width: 749px) {
  .custom-features .features-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
  }

  .custom-features {
    padding: 30px 15px;
  }
}
{% endstylesheet %}

<div class="custom-features">
  <div class="features-grid">
    {% for block in section.blocks %}
      <div class="feature-block">
        <div class="svg-icon">
          {{ block.settings.icon_svg }}
        </div>
        <h3>{{ block.settings.title }}</h3>
        <p>{{ block.settings.description }}</p>
      </div>
    {% endfor %}
  </div>
</div>
