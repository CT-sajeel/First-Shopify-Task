{%- comment -%}
  File Name: ct-blogs
  Section Name: CT Blogs
  Author: Sajeel Shafqat
  Created: 07-08-2025
  Description: This is a section to add Blogs and can link to that blog.
{%- endcomment -%}  
 
{% style %}
#shopify-section-{{ section.id }} .skin-needs-section {
  padding: 40px 80px;
}

#shopify-section-{{ section.id }} .skin-needs-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 30px;
}

#shopify-section-{{ section.id }} .ct-blogs {
  background-color: #fff;
  text-align: left;
  display: flex;
  flex-direction: column;
  height: 100%;
}

#shopify-section-{{ section.id }} .ct-blogs .content {
     padding: 30px 0px 20px 0px;
    background: #fff;
    min-height: 250px;
}

#shopify-section-{{ section.id }} .ct-blogs h3 {
    color: #000000;
    font-weight: 500;
    line-height: 24px;
    font-size: 16px;
    margin-bottom: 20px;
    margin-top: 0px;
    font-family: "Poppins Medium";
}   

#shopify-section-{{ section.id }} .ct-blogs p {
    letter-spacing: normal;
    color: #000000;
    font-size: 16px;
    margin-bottom: 15px;  
}

#shopify-section-{{ section.id }} .ct-blogs a {
  font-weight: bold;
  color: #42a7c6;
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  gap: 5px;
}

#shopify-section-{{ section.id }} .ct-blogs a:hover {
  text-decoration: underline;
}

@media screen and (max-width: 749px) {
  #shopify-section-{{ section.id }} .skin-needs-section {
  padding: 40px 15px;
  }
  #shopify-section-{{ section.id }} .skin-needs-grid {
    grid-template-columns: repeat(1, 1fr);
    gap: 25px;
  }
} 
{% endstyle %}

<div class="skin-needs-section">
  <div class="skin-needs-grid">
    {% for block in section.blocks %}     
      <div class="ct-blogs">
       {% comment %} Image block using Dawn-compatible layout {% endcomment %}
        <div class="image-with-text__media-item _grid__item">
          <div
            class="image-with-text__media image-with-text__media--adapt global-media-settings
              {% if block.settings.image != blank %} media{% else %} image-with-text__media--placeholder placeholder{% endif %}"
            {% if block.settings.image != blank %}
              style="padding-bottom: {{ 1 | divided_by: block.settings.image.aspect_ratio | times: 100 }}%;"
            {% endif %}
          >
            {% if block.settings.image != blank %}
              {% assign widths = '165, 360, 535, 750, 1070, 1500' %}
              {% capture sizes %}
                (min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 100 | divided_by: 3 }}px,
                (min-width: 750px) calc((100vw - 130px) / 3),
                calc((100vw - 50px) / 1)
              {% endcapture %}
              {{
                block.settings.image
                | image_url: width: 1500
                | image_tag: sizes: sizes, widths: widths, alt: block.settings.title
              }}
            {% else %}
              {{ 'detailed-apparel-1' | placeholder_svg_tag: 'placeholder-svg' }}
            {% endif %}
          </div>
        </div>

       {% comment %}Text content {% endcomment %}
        <div class="content">
          <h3>{{ block.settings.title }}</h3>
          <p>{{ block.settings.description }}</p>
          {% if block.settings.link != blank %}
            <a href="{{ block.settings.link }}">Read More</a>
          {% endif %}
        </div>
      </div>
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "CT Blogs",
  "tag": "section",
  "class": "ct-skin-needs-section",
  "settings": [],
  "blocks": [
    {
      "type": "need",
      "name": "Skin Need Block",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Skin Concern"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Description"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Button Link"
        }
      ]
    }
  ],
  "max_blocks": 6,
  "presets": [
    {
      "name": "CT Blogs",
      "blocks": []
    }
  ]
}
{% endschema %}